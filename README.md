# Python Web Server Demo
A simple python web server demo I made while learning uvicorn, FastAPI, and python.

# Usage
Open the virtual environment inside the master branch directory and start uvicorn with main.py using the home function.
